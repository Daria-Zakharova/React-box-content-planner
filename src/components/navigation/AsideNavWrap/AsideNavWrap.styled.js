import styled from "@emotion/styled";

export const AsideNav = styled.div`
    position: absolute;
    left: calc(100% - 30px);
    top: 40px;
    display: flex;
    flex-direction: column;
    gap: 50px;
`;